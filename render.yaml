services:
  - type: web
    name: sentiment-api
    env: docker
    dockerfilePath: Dockerfile
    plan: starter
    regions:
      - oregon
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /
